name: Docker Action Recipe
description: Greet Someone
inputs:
  who-to-greet:
    description: Who to greet
    required: true
    default: world
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.who-to-greet }}